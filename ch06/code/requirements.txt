# 제6장 시스템 다이내믹스 실습 환경

# 핵심 라이브러리
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
networkx>=3.0

# 시스템 다이내믹스 (선택)
# pysd>=3.0.0  # Vensim/XMILE 모델 실행
# Note: PySD는 추가 의존성이 많아 순수 Python 구현으로 대체 가능

# 시각화 보조
seaborn>=0.12
